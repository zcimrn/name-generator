[project]
name = "name-generator"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["pykakasi>=2.3.0", "regex>=2026.5.9", "wordfreq>=3.1.1"]

[project.scripts]
name-generator = "name_generator.main:main"

[build-system]
requires = ["uv-build>=0.11.0,<0.12.0"]
build-backend = "uv_build"
